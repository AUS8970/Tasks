import React from 'react';

const InProgress = ({ tasks }) => {

  // const inProgressTasks = await fetchTasks("In Progress");

  return (
    <div>
      
    </div>
  );
};

export default InProgress;